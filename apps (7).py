from django.apps import AppConfig


class StaffingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.staffing'
    verbose_name = 'Staff, events, check-in, payroll'
