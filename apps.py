from django.apps import AppConfig


class FormbuilderConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.formbuilder'
    verbose_name = 'Form / question builder'
